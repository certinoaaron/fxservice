version: "3.5"
services:
  fxservice:
    image: fxservice:latest
    ports:
      - "6101:6101"
    environment: 
        DDTRACE_HOST: "dd-agent"
      
# defined to connect to local platform network
networks:
  default:
      external:
          name: filenetwork